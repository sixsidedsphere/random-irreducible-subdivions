{
  "name": "@push-heal/playground",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run build:local",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "build:local": "PUBLIC_BASE_PATH=/ vite build",
    "build:pages": "PUBLIC_BASE_PATH=${PUBLIC_BASE_PATH:-/random-irreducible-subdivions/playground/} vite build"
  },
  "dependencies": {
    "push-heal-rectangulation": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^5.0.0"
  }
}
